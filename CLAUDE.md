# Startup Brain

## What This Is
AI-powered knowledge management for a 2-person startup. See `docs/SPEC.md` for full specification.

## Tech Stack
- Python 3.12+, Streamlit, MongoDB Atlas (pymongo), Anthropic API (anthropic SDK)
- NO LangChain, NO LlamaIndex, NO heavy frameworks. Direct API calls only.
- Minimal dependencies. Check requirements.txt before adding anything.

## Architecture
- Streamlit app at `app/main.py` (entry point)
- Layout: Two-view tab navigation (Chat | Dashboard) with persistent top bar
- No sidebar — all content in main area, sidebar hidden via CSS
- Navigation via `session_state.active_view` (`st.radio` styled as tabs), not `st.tabs()`
- Parser functions shared in `app/components/_parsers.py` — all parsing functions call `_normalize_headers()` to strip trailing whitespace from markdown header lines before regex matching
- Services in `services/` — each service is a single-purpose module
- LLM prompts in `prompts/` as markdown files — loaded at runtime, never hardcoded
- Two-brain architecture: **Pitch Brain** (`documents/pitch_brain.md`) for investor narrative, **Ops Brain** (`documents/ops_brain.md`) for operational knowledge — both git-tracked, MongoDB is authoritative source of truth for reads (local file is synced cache, enables seamless switching between localhost and Render)
- Brain-aware services: all document/storage functions accept `brain="pitch"|"ops"` parameter (default "pitch" for backward compat). MongoDB queries use `$or` pattern with `{"$exists": False}` to include pre-migration docs that lack the `brain` field.
- Two brain variables with different semantics: `active_brain` is the **write target** (set by top bar toggle, always `"pitch"` or `"ops"`, never `"both"`); `chat_brain_context` is the **read scope** (set by chat context selector, can be `"pitch"`, `"ops"`, or `"both"`). Write operations must use `active_brain`; read/display operations use `chat_brain_context`.
- Pitch-only document helpers (`_add_decision`, `_add_dismissed`) accept `brain=` and skip for non-pitch — ops brain has no Decision Log or Dismissed Contradictions sections.
- All state management via `st.session_state` — Streamlit re-runs on every interaction
- Pitch state machine: `chat → ingesting → confirming_claims → checking_consistency → resolving_contradiction → done`
- Ops state machine: `chat → ops_ingesting → ops_confirming → ops_done` (no consistency check)
- `app/main.py` inserts project root into `sys.path` at startup — required because Streamlit only adds the script's directory (`app/`) to the path, not the project root
- Auth gate at top of `app/main.py` — cookie-based login when `APP_USERNAME` + `APP_PASSWORD` env vars are set, requires explicit `DISABLE_AUTH=true` to skip in production (auto-detected via `RENDER`/`PORT` env vars), skipped in local dev when env vars unset
- Ingestion lock via `services/ingestion_lock.py` — MongoDB-based atomic lock prevents concurrent ingestion, 30-min stale timeout for crash recovery. Lock ownership revalidated at `batch_commit()` time via `check_lock()` — prevents stale session from writing after lock was stolen by another user during network interruption.
- Document write lock via `services/ingestion_lock.py` — short-lived MongoDB lock (2-min timeout) prevents concurrent read-modify-write on living document across chat corrections, feedback, hypothesis updates, and ingestion batch_commit. **Not re-entrant** — never call `update_document()` (which acquires its own lock) while holding an outer doc lock; instead, call `update_document()` first, then acquire the lock separately for additional writes. Lock has **ownership verification**: `acquire_doc_lock()` returns a `lock_id` string (UUID), which must be passed to `release_doc_lock(lock_id)` — only the owner can release. Both lock documents initialized at startup via `ensure_lock_document()` + `ensure_doc_write_lock()`.

## Session Rollback
- `from services.deferred_writer import rollback_last_session; rollback_last_session()` — rolls back the most recent session (reverts the correct brain document to previous git version, mirrors to MongoDB first, then writes to file, then deletes session + claims from MongoDB, git commits the revert). MongoDB mirror written before file (source of truth on Render's ephemeral FS).
- `services/mongo_client.py` also exposes `delete_many(collection, query)` and `get_latest_session(brain="")` for manual cleanup

## Key Conventions
- Use `@st.cache_resource(ttl=300)` for MongoDB connections and the Anthropic client — TTL prevents permanent `None` caching if unavailable at startup
- Every Claude API call goes through `services/claude_client.py` which handles cost tracking, Sonnet/Opus routing, and error sanitization
- Shared XML parsing utility: `from services.claude_client import extract_xml_tag` — used by ingestion.py, consistency.py, feedback.py (eliminates duplication). Applies `html.unescape()` automatically so LLM-echoed entities (`&lt;`, `&amp;`) are decoded before storage — downstream consumers can safely `escape_xml()` without double-encoding
- Prompts are markdown files in `/prompts` — read them with open(), never inline them
- Use XML tags for structured LLM input/output
- Git commit living documents after every update with descriptive message (no-ops gracefully when git unavailable, e.g. Render)
- Living document file writes are atomic: `tempfile.mkstemp()` + `os.replace()` prevents partial-write corruption on crash
- MongoDB is the authoritative source for living document reads — `read_living_document()` checks MongoDB first, syncs local file as cache, falls back to file when MongoDB is unavailable. This enables seamless switching between localhost and Render without document drift. MongoDB mirror is written before the file on writes. If MongoDB mirror fails, file write is **aborted** to prevent file-ahead desync (V1-B fix).
- Session types (defined in `app/state.py:SESSION_TYPES`) flow through extraction, consistency, pushback, audit, and storage
- All new service function parameters must default to empty string/None for backward compatibility
- Auth: shared credentials via `APP_USERNAME` + `APP_PASSWORD` env vars. Cookie-based 7-day sessions (`streamlit-cookies-controller`). Auth skipped in local dev when env vars unset; production (Render) requires credentials or explicit `DISABLE_AUTH=true`.
- Ingestion lock: MongoDB-based (`services/ingestion_lock.py`). Only one user can ingest at a time. Lock auto-expires after 30 min (crash recovery). Top bar shows lock status.
- Document write lock: MongoDB-based (`services/ingestion_lock.py`). Short-lived lock (2-min timeout) for living document writes. Prevents concurrent corruption from chat corrections, feedback, hypothesis, and batch_commit.
- Session IDs: UUID-based (`uuid.uuid4()`), not Python memory addresses. Stored in `st.session_state._lock_session_id`.
- Error messages: sanitized for users (generic messages), full details logged server-side only. Prevents MongoDB URI / path leakage.

## File Ownership (for parallel agent work)
When splitting tasks across agents, avoid overlapping file edits:
- **Frontend layout**: `app/main.py`, `app/state.py`
- **Frontend components**: `app/components/top_bar.py`, `app/components/dashboard.py`, `app/components/ops_dashboard.py`, `app/components/chat.py`, `app/components/claim_editor.py`, `app/components/progress.py`, `app/components/login.py`
- **Theme/styles**: `app/components/styles.py`
- **Parsers**: `app/components/_parsers.py` (shared parsing functions), `app/components/sidebar.py` (legacy re-export shell)
- **Backend services**: `services/*.py`
- **Prompts**: `prompts/*.md`
- **Tests**: `tests/test_*.py`

## Testing
- Test transcripts in `tests/test_transcripts/`
- Run unit tests: `python -m pytest tests/ -m "not integration"`
- Run integration tests: `python -m pytest tests/test_integration.py -v` (requires API key + MongoDB — keys in `.streamlit/secrets.toml`, must export to env vars for pytest)
- 1024 unit tests + 56 integration tests across 29 test files, all unit tests run fully offline with mocks

## Deployment
- **Render** (primary): `render.yaml` Blueprint — free tier, auto-deploy from GitHub
  - Live at: `https://startupbrain.onrender.com`
  - Start command: `streamlit run app/main.py --server.port $PORT --server.address 0.0.0.0 --server.headless true`
  - Ephemeral filesystem: living documents auto-recover from MongoDB on restart
  - Git commits no-op (no git repo on Render)
  - Set env vars in Render dashboard: `ANTHROPIC_API_KEY`, `MONGODB_URI`, `APP_USERNAME`, `APP_PASSWORD`
  - MongoDB Atlas Network Access must allow `0.0.0.0/0` (Render uses dynamic outbound IPs)
- **Streamlit Community Cloud** (legacy): still works, secrets via Cloud dashboard
- Entry point: `app/main.py`
- Required secrets/env vars: `ANTHROPIC_API_KEY`, `MONGODB_URI`
- Optional secrets/env vars: `APP_USERNAME`, `APP_PASSWORD` (enables login gate), `DISABLE_AUTH` (set to `true` to skip auth in production)

## Current Status (as of 2026-03-08)

### Implementation: Complete
All 24 sections of `docs/SPEC.md` are implemented plus brain split architecture. The system is production-ready for daily use.

**Two-brain architecture:** Pitch Brain (`documents/pitch_brain.md`) holds the curated investor narrative (13 pitch sections + Decision Log + Dismissed Contradictions). Ops Brain (`documents/ops_brain.md`) holds operational knowledge (contacts, hypotheses, risks, assumptions, questions, feedback, hiring, scratchpad). Brain toggle in top bar switches context. Migration script at `scripts/migrate_brain_split.py`.

**Core pipeline:** Pitch Brain: full ingestion (transcript → claim extraction with entity extraction → human confirmation → consistency check → doc update), 3-pass consistency engine (P1+P2 Sonnet, P3 Opus on Critical only), diff-and-verify living document updates, git auto-commit after every update. Ops Brain: simplified pipeline (transcript → extraction with ops_extraction prompt → human confirmation → direct doc update using ops_diff_generate prompt, no consistency check). Both pipelines extract structured `entities` (people, companies, products, concepts) per claim during extraction, stored in MongoDB for future entity-based retrieval.

**UI:** Dark command center theme (Vercel/Raycast inspired). Two-view layout: Chat (default) with quick command chips, Dashboard (brain-dependent: pitch shows 13-section card grid + panels, ops shows contacts/hypotheses/risks/feedback). Persistent top bar with Brain toggle (Pitch/Ops), Ingest button (routes to correct pipeline), Audit button (pitch only), and API cost + RAG health status pills. No sidebar. Chat has brain context selector (Pitch/Ops/Both) for system prompt routing. Conversational chat with query classification and streaming, HTML/CSS step indicators, claim editor with inline editing and `ops_mode` parameter for brain-specific button rendering.

**Features:** Session type categorization through entire pipeline, whiteboard photo processing (vision), feedback pattern detection, evolution narratives, pitch material generation (Opus), cost tracking with budget alerts, book framework cross-check via .md upload in chat, direct corrections with informational consistency check (brain-aware — routes to active brain), contradiction resolution writing Decision Log and Dismissed Contradictions entries, scratchpad notes via chat prefix (`note:`, `remember:`, `jot:`, `fyi:`) saved to MongoDB only (no doc update, surfaced in chat system prompt across all brain contexts), hypothesis tracking via chat prefix (`hypothesis:`, `validated:`, `invalidated:`) or dashboard form, Socratic system prompt with context surfacing and feedback echo, dashboard tensions indicator (changelog churn, dismissed contradictions, decisions under evaluation), 'challenge' query classification routing to Opus, 3 quick command chips (note, hypothesis, contact) below chat input for prefix discoverability, full context export (living doc + session history + claims as single MD — available for both Pitch and Ops brains via dashboard), session rollback command, shared-credential auth with cookie persistence, ingestion lock + document write lock for concurrent access.

**Security:** Auth hardened for production (requires credentials or explicit `DISABLE_AUTH=true`), HMAC cookie signing with PBKDF2-stretched key (100k iterations, app-specific salt, no fallback key), sanitized error messages (no URI/path/API key leakage — all API and MongoDB errors use `logging.error()` server-side + generic user-facing messages), UUID-based session IDs, XML-escaped living document content in all LLM prompts (system prompt, scratchpad notes, book frameworks, conversation history — including first-turn messages — all escaped via `escape_xml()`), `re.escape()` on all user-provided values in MongoDB `$regex` queries (prevents ReDoS).

**Multi-user safety:** Two-tier locking — long-lived ingestion lock (30-min timeout) for full pipeline, short-lived document write lock (2-min timeout) for individual writes (including dashboard hypothesis forms, chat hypothesis status updates, and contradiction resolution fallback path). Lock documents initialized at app startup via `ensure_lock_document()` + `ensure_doc_write_lock()` (prevents first-use race conditions on fresh deployments). Doc write lock has **ownership verification** — `acquire_doc_lock()` returns a UUID `lock_id`, `release_doc_lock(lock_id)` filters by `session_id` so only the owner can release (mirrors the ingestion lock pattern). Atomic lock operations via `find_one_and_update` with `ReturnDocument.AFTER`. DeferredWriter checkpoints track lock ownership to prevent cross-user recovery hijacking. Crash recovery detects document drift (warns if living document changed since checkpoint was saved). Dismissed contradictions properly filtered in consistency engine Pass 2. Doc lock acquisition resilient to transient MongoDB errors (try/except with retry). Ops dashboard hypothesis form shows persistent warning on lock failure (prevents silent text loss). DeferredWriter deletes checkpoint on doc-lock failure to prevent duplicate claims on resume. Orphaned sessions cleaned up on batch_commit failure.

**LLM integration hardening:** Anthropic client cached via `@st.cache_resource(ttl=300)` (re-checked every 5 min, prevents permanent `None` caching). Consistency engine detects API errors and surfaces "check failed" instead of silently producing "no contradictions". All 3 consistency passes, evolution narratives, and pitch material generation are brain-aware. Pass 2 input properly wrapped in `<pass1_output>` tags. Book framework content excluded from recall/historical query types to avoid inflating system prompts. Ops brain uses dedicated `ops_diff_verify.md` prompt for diff verification (pitch uses `diff_verify.md`). RAG health monitor is brain-aware — shows claim count for the active brain. Top bar cache invalidated on brain switch (prevents stale RAG pill).

**Data integrity, locking & security hardening (2026-03-05 review — 15 bugs fixed):** Deadlock in `_resolve_contradiction()` fixed — `update_document()` (which manages its own doc lock) no longer called inside an outer doc lock scope; each action path ("update"/"keep"/"explain") acquires the lock only for its own read-modify-write. Prompt injection on first-turn chat message fixed (`escape_xml()` applied unconditionally). Atomic file writes via `tempfile.mkstemp()` + `os.replace()` prevent partial-write corruption. MongoDB mirror written before file in both `update_document()` and `batch_commit()` (source of truth on Render's ephemeral FS). `re.escape()` applied to all user-provided values in `$regex` MongoDB queries (prevents ReDoS). Lock documents initialized at startup (prevents first-use race on fresh deployments). HMAC cookie key uses PBKDF2 with 100k iterations. Rollback reordered: file write before MongoDB deletes, session deleted before claims (safer failure ordering). Crash recovery logs errors instead of bare `pass`. Quick note checks `insert_claim` return value. Feedback themes update failure logged. Lock error messages no longer include raw exception text. Crash recovery UI warns when living document has changed since checkpoint was saved. Batch_commit cleans up orphaned sessions on failure.

**State machine & session state hardening (2026-03-04 review — 16 bugs fixed):** `@st.cache_resource(ttl=300)` on MongoDB and Anthropic clients prevents permanent `None` caching on startup failure. Cancel button added to `render_contradiction_resolution()` (was the only pipeline stage without an escape hatch). `_consistency_failed` flag prevents re-running expensive LLM calls on every Streamlit rerun after an exception (explicit Retry/Cancel instead). `_ops_commit_failed` flag prevents ops ingestion auto-retry on every rerun (explicit Retry button instead). `uploaded_file.getvalue()` instead of `.read()` for whiteboard (safe after `st.image()` consumes pointer). `evolution_result`, `_context_export_data`, `_ops_context_export_data` cleared in `reset_ingestion()` and brain switch handler (prevents stale cross-brain data). `reset_ingestion()` no longer forces `active_view = "chat"` (preserves Dashboard tab). Lock-blocked "Back to Chat" paths use `reset_ingestion()` instead of `set_mode("chat")` (cleans up stale `_lock_session_id`). Book framework file only re-decoded when filename changes (skips redundant decode on every rerun). Dead `ingestion_status` key removed. `_batch_commit_failed`, `_consistency_failed`, `_ops_commit_failed` added to `init_session_state()` defaults.

**Brain isolation integrity (2026-03-04 review — 26 bugs fixed):** DeferredWriter threads `self.brain` to all callees (`verify_diff`, `apply_diff`, `_add_decision`, `_add_dismissed`). Direct corrections use `active_brain` (write target), not `chat_brain_context` (read scope). Recall context filters feedback by brain. Feedback field reads use correct MongoDB field name (`feedback_text`). `get_recurring_themes()`/`should_alert()` accept `brain=` parameter. Crash recovery syncs both `active_brain` and `chat_brain_context`. `_add_section` has brain-aware fallback insertion (ops: before Scratchpad Notes; pitch: inside Current State). `_add_decision`/`_add_dismissed` guard against non-pitch brain (ops has no Decision Log/Dismissed Contradictions). `get_latest_session()` accepts `brain=` filter. Feedback themes written back to MongoDB after pattern detection. Dashboard hypothesis form inserts claim into MongoDB for query parity. `run_ops_ingestion()` parameterized with `brain=` (default "ops").

**Infrastructure:** Vector search code ready (`vector_search_text()`, upgraded `_get_rag_evidence()`), time-based fallback on free tier (retrieves 10 most recent claims), RAG health monitor (warns at 200 claims). MongoDB indexes on `brain` field for claims/sessions/feedback collections (added in bootstrap.py for `$or` query performance at scale). System prompt token budget: living document capped at 120K chars (~30K tokens) to prevent context window overflow. Streaming cost tracking: estimates input tokens from prompt length on stream failure (prevents systematic under-counting). Vector search `path` parameter aligned with Atlas autoEmbed index config (`claim_text`, not `claim_text_embedding`). Render deployment live at `https://startupbrain.onrender.com` (free tier), ephemeral filesystem handled by MongoDB document recovery, git no-op when not in a repo.

**Meta-pattern hunt (2026-03-06 — 22 findings, 8 files fixed):** 7-agent parallel review hunting structural bug siblings across 7 meta-patterns. D3 (6 findings): `write_living_document` before `upsert_living_document` in hypothesis add/status update and all 3 contradiction resolution branches — swapped to MongoDB-first (source of truth on Render's ephemeral FS). D4 (5 findings): unchecked `store_session`/`insert_claim` return values in `batch_commit`, ops dashboard, contact notes, whiteboard — added return checks, `batch_commit` returns `success=False` when session not saved. D5 (6 findings): broad `except: pass` hiding stuck checkpoints, stuck ingestion locks, silent claim/hypothesis sync failures — added `logging.error`/`logging.warning`, `batch_commit` always deletes checkpoint on failure (prevents stuck recovery prompts). D6 (1 finding): inline `doc_write_lock` init in `main.py` calling private `_get_lock_collection` — extracted to `ensure_doc_write_lock()` in `ingestion_lock.py`. D7 (4 findings): `release_doc_lock()` had no ownership verification — added `session_id` field to lock document, `acquire_doc_lock()` now returns `lock_id` string (UUID), `release_doc_lock(lock_id)` filters by owner (mirrors ingestion lock pattern); all 9 call sites updated. D1 (deadlocks) and D2 (security bypasses): clean — no new findings.

**Clone drift, prompt/parser & enum audit (2026-03-06 — 6 findings, 5 files fixed):** 5-agent parallel review across clone drift, shared prompts, shared services, prompt-parser consistency, and enum exhaustiveness. Clone drift (1): `ops_diff_verify.md` had phantom action names (`ADD_RISK`, `ADD_ASSUMPTION`, `ADD_QUESTION`) not in ops allowed actions — replaced with valid-action checklist. Shared services (2): `_add_changelog()` and `_add_feedback()` in `document_updater.py` had no `brain=` parameter — ops changelogs silently dropped (regex assumed pitch `###` + `**Changelog:**` structure), ops feedback misplaced (appended after `### Individual Feedback` boundary instead of under it) — added `brain=` with ops-specific regex for both. Shared prompts (2): `generate_pitch_materials()` accepted `brain="ops"` unchecked (would generate VC pitch from ops contacts/risks doc) — added pitch-only guard; `run_audit()` had no code-level brain guard (prompt assumes "Current State" pitch structure) — added pitch-only guard. Prompt-parser (1): audit `<evidence><citation>` tags produced by LLM but never parsed — added citation extraction to discrepancy loop. Enum exhaustiveness: all clean (session types freeform, brain values properly guarded, query classifications fully mapped, all 9 pipeline modes handled with catch-all, claim types freeform).

**UX & pipeline robustness audit (2026-03-07 — 28 findings, 6 files fixed):** 6-agent parallel review across 6 meta-patterns. U1 (4 findings): pipeline advancing without checking previous step — doc update failure now raises `RuntimeError` caught by `_consistency_failed` guard; contradiction resolution checks `apply_document_update_deferred()` return before advancing; ops pipeline warns on zero extracted claims instead of silently advancing. U2 (3 findings): retry duplication — `batch_commit()` now tracks `_committed_session_id` to skip re-inserting on retry; checkpoint preserved on doc-changed failure (enables idempotent retry). U3 (7 findings): developer jargon in UI — raw `writer.stage` fallback → "In progress"; Atlas M10+ tooltip → "Contact your developer"; "Deep Analysis (Opus)" → "Deep Analysis"; raw exception text replaced with actionable guidance; "MongoDB is unavailable" → "Memory storage unavailable"; Session ID hidden in Debug expander; ops partial result distinguishes 0-items (error) vs >0 (warning). U4 (1 finding): Ingest button disabled without `help=` tooltip during pipeline — added conditional tooltip. U5 (2 findings): direct correction in Ops Brain fired 2 wasted Sonnet consistency check calls — guarded with `if brain == "pitch"`; `generate_evolution_narrative()` added pitch-only early return. U6 (10 findings): 8 bare `except: pass` blocks now log with `logging.warning`/`logging.error`; ops failure stores `_ops_commit_fail_msg`; hypothesis sync message corrected from "pending" to "failed".

**Systematic failure path audit (2026-03-07 — 26 findings, 10 files fixed):** 5-sweep parallel review auditing every failure path in the codebase: exception handlers, unchecked write returns, retry idempotency, pipeline failure gating, and premature success messages. Sweep 1 (7 findings): 6 silent `except: pass` blocks now log at appropriate levels (vector search fallback, RAG threshold, disk recovery, lock race, cookie read/write, vector_search_text); `batch_commit` exception now tracks `doc_write_succeeded` flag for accurate `document_updated` return. Sweep 2 (9 findings): `save_checkpoint()` now returns bool and logs failure; `_resolve_contradiction` fallback checks `update_document` return and aborts on failure; `delete_one`/`delete_pending_ingestion` returns checked and logged; `upsert_living_document` and `update_hypothesis_status` returns checked in hypothesis paths; rollback write order fixed (MongoDB first); ops `success` computation requires session storage. Sweep 3 (5 findings): `_committed_session_id` persisted to DeferredWriter checkpoint (prevents duplicate session+claims on crash recovery resume); claims dedup before insert via `find_many` (prevents duplicates on retry); ops ingestion accepts `existing_session_id` parameter for safe retry; doc-lock-failure branch sets `_committed_session_id` after `store_session`. Sweep 4 (6 findings): `batch_commit` `success` now requires `claims_stored > 0` when claims expected; ops `success` requires `session_id` not None; consistency API error distinguished from "no contradictions" in UI; crash recovery routes `stage="consistency_check"` back to `checking_consistency` instead of `done`. Sweep 5 (3 findings): success banner gated on `_batch_committed` flag (not in-memory LLM result); "already updated" info uses `commit_result.get("document_updated")` not `pipeline_result`; whiteboard filename tracked to clear stale content on re-upload. 2 test assertions updated for corrected ops_ingestion success semantics.

**Structured entity extraction (2026-03-07 — inspired by Google's Always-On Memory Agent):** Extraction prompts (`extraction.md`, `ops_extraction.md`) now instruct the LLM to extract `<entities>` (people, companies, products, concepts) per claim alongside existing `<topic_tags>`. Parser in `extract_claims()` parses entities using the same `re.findall` + `extract_xml_tag` pattern as topic_tags. `store_confirmed_claims()` persists entities to MongoDB via explicit `.get("entities", [])` in the whitelist (entities would be silently dropped without this — the function uses field-by-field extraction, not dict spread). Old claims without entities gracefully default to `[]` via `.get()`. Ad-hoc claim paths (quick notes, contacts, hypotheses) don't produce entities — consumers handle absence via `.get("entities", [])`. Enables future entity-based MongoDB queries (`{"entities": {"$in": ["CompanyX"]}}`) without requiring Atlas vector search.

**Meta-pattern hunt addendum (2026-03-07 — 16 findings, 7 files fixed):** 6-agent parallel review hunting structural bug siblings across 6 meta-patterns (F1–F6). F1 (1 finding): `_changes_applied` not serialized in DeferredWriter checkpoint — crash recovery showed "Sections updated: 0" after resume; added to `to_checkpoint()`/`from_checkpoint()`. F2 (4 findings): `document_updated` return used `doc_changed` (intent) instead of `doc_write_succeeded` (reality) — fixed in happy-path return; consistency `run_consistency_check()` now returns `api_error: True` on API failure (done screen shows "Check failed" instead of "None"); `rollback_last_session` now checks `delete_one`/`delete_many` return values and gates `success` on actual deletion; `update_one()` in mongo_client now checks `modified_count` instead of returning `True` unconditionally. F4 (3 findings): partial claim insert on retry permanently dropped remaining claims — replaced all-or-nothing session-level guard with count-based partial retry that inserts only missing claims; doc-lock failure branch now saves checkpoint with `_committed_session_id` (prevents duplicate session on retry); chat hypothesis/contact `insert_claim` calls now have `find_one` dedup guards. F5 (4 findings): `DeferredWriter.rollback()` (Discard) now cleans up committed session/claims from MongoDB and reverts already-written documents; `rollback_last_session` now deletes feedback records; unknown checkpoint stages show warning and auto-discard instead of routing to `batch_commit`. F6 (4 findings): `_whiteboard_filename`, `_consistency_fail_msg`, `_ops_commit_fail_msg` now cleared in `reset_ingestion()`; `_ops_hyp_lock_failed` added to `init_session_state()` defaults.

**End-to-end data integrity audit (2026-03-07 — 11 findings, 6 files fixed):** 3-agent parallel review tracing actual data through 6 full pipelines (transcript→dashboard, chat→LLM→display, scratchpad lifecycle, hypothesis lifecycle, contact lifecycle, export). B1 (root cause): `extract_xml_tag()` in `claude_client.py` never called `html.unescape()` — if LLM echoed XML entities (`&lt;50`) inside response tags, they propagated verbatim through claims, consistency checks (double-escaping to `&amp;lt;`), contradiction display, and into the living document. Fixed by adding `html.unescape()` to `extract_xml_tag()` return. T4-1/T4-2: hypothesis status update regex required exact match (`\*\*{fragment}\*\*`) while MongoDB used substring `$regex` — partial fragments like `validated: SMBs will pay $49` silently failed in document but succeeded in MongoDB, causing permanent desync. Fixed with prefix-match regex (`\*\*{fragment}[^*]*\*\*`). T4-3: status update returned "Hypothesis updated" unconditionally even when MongoDB sync failed — added `synced` check with parenthetical warning. T5-2: `_update_contact` silently no-opped when LLM normalized accented chars — added `logging.warning`. F1: contradiction display applied `html.escape()` to values that might contain entities — added `html.unescape()` before `html.escape()`. 2a-1: recall query classifier missed natural phrasings (`"did we discuss"`, `"did we talk about"`) — added keywords. T6-1/T6-2/T6-4: export claims not filtered by brain, multi-line claim text broke markdown structure, sessions sorted by ingestion time not meeting date — all fixed. 2 test assertions updated for new `html.unescape()` behavior.

**System invariants & compound failures audit (2026-03-07 — 10 findings, 8 files fixed):** 6-agent parallel review auditing 7 system invariants, 6 compound failures, and 5 cross-component assumption violations. V1-B (2 files): MongoDB mirror failure in `update_document()` and `batch_commit()` previously continued to write the file — on Render restart, stale MongoDB version was recovered causing silent data loss. Fixed: both paths now abort when MongoDB mirror fails, preventing file-ahead desync. V1-C/C5: recovery write-back in `read_living_document()` used non-atomic `open()` — crash mid-recovery left partial file that bypassed `if not content:` check permanently. Fixed: now uses atomic `write_living_document()`. C4: `_get_rag_evidence()` returned `[]` silently when MongoDB was down — Pass 3 Opus ran without historical evidence, no warning shown. Fixed: added `evidence_missing` flag to consistency results, surfaced in done screen warning. V4-B: Decision Log entry written even when `apply_document_update_deferred` returned `changes_applied=0`. Fixed: gated on `changes_applied > 0`. S2: `_apply_contact()` showed "Contact noted" when `insert_claim` failed silently. Fixed: added `claim_synced` check with parenthetical warning. L1: ingestion lock cached in `_lock_acquired` session state — never rechecked at `batch_commit` time. Fixed: `batch_commit()` now revalidates lock ownership via `check_lock()` before writing. Parser fragility: all 8 parsing functions in `_parsers.py` required exact header whitespace — trailing space broke all dashboard cards silently. Fixed: added `_normalize_headers()` that strips trailing whitespace from header lines. Feedback miscategorization: unrecognized orgs defaulted to `"vc"` bucket. Fixed: default bucket changed to `"other"`, added to both dashboards. 3 test assertions updated for corrected behavior. Compound failures C1 (double-escaping × hypothesis), C2 (stale cache × brain switch), C3 (checkpoint × retry), C6 (lock × cold start × concurrent) all DENIED after code trace — prior fixes already addressed these.

**Temporal degradation & scale hardening (2026-03-07 — 5 fixes across 5 files):** Code review Pass 7b audited 6 temporal degradation scenarios and 8 adversarial user scenarios. Fixed: (1) RAG health message said "50 most recent claims" but code uses `limit=10` — corrected message. (2) Missing MongoDB indexes on `brain` field for claims/sessions/feedback + `claim_type` for claims — added to `bootstrap.py` (prevents full collection scans at 500+ claims). (3) Vector search `path` default was `"claim_text_embedding"` but Atlas autoEmbed index uses `"claim_text"` — fixed mismatch in `mongo_client.py` (dead code today, ready for M10+ upgrade). (4) System prompt had no token budget — living document could grow unbounded until hitting 200K context limit with opaque "temporarily unavailable" error. Added 120K char cap (~30K tokens) with truncation warning in `chat.py`. (5) Streaming API failures logged `tokens_in=0` — added input token estimation from prompt length (~4 chars/token) so partial streams are not recorded as $0 in `claude_client.py`. Adversarial scenarios verified safe: prompt injection blocked (escape_xml on all inputs), concurrent tabs handled (ingestion lock), brain toggle debounced by Streamlit, stale tabs recovered via checkpoint, empty inputs guarded.

**Document freshness check in DeferredWriter (2026-03-07 — 1 race condition fixed, 1 checkpoint bug fixed):** `DeferredWriter.batch_commit()` snapshots the living document at pipeline initialization but writes it 2-5 minutes later. Concurrent writes (hypothesis, contact, correction) via `doc_write_lock` during the pipeline window were silently overwritten by the stale snapshot. Fix: `batch_commit()` now computes `original_doc_hash` (SHA-256) at initialization, serialized in checkpoints. At commit time, re-reads the current document and compares hashes. If changed, re-generates diffs against the current document (Option A: one extra Sonnet call, ~$0.02) via `generate_diff()` + `verify_diff()` + `apply_diff()`. If re-diff verification fails, aborts doc write but still stores session + claims, and deletes the checkpoint (prevents duplicate session on Resume). `_build_claims_summary()` helper builds the re-diff input from confirmed claims. Backward compatible: old checkpoints without `original_doc_hash` recompute it from `original_doc`. The fallback `_resolve_contradiction()` path (non-deferred) was verified safe — `update_document()` acquires its own lock and reads fresh. Adversarial verification (35 points): all PASS except the checkpoint bug above (now fixed). `apply_document_update_deferred()` confirmed in-memory only — contradiction resolution during pipeline does NOT cause false conflicts.

**MongoDB-first document reads (2026-03-08):** `read_living_document()` now checks MongoDB first (authoritative source), syncs local file as cache, falls back to file when MongoDB unavailable. Enables seamless switching between localhost and Render without document drift. Safety guard: refuses to overwrite local file if MongoDB content is >50% shorter (prevents accidental truncation from corrupt data). File write skipped when content matches (no unnecessary disk I/O).

**LaTeX escaping in chat (2026-03-08):** Dollar amounts like `$150B` were rendered as LaTeX math by Streamlit. All dynamic `st.markdown()` calls in chat now use `_escape_latex()`. Streaming chunks escaped for live display, unescaped before storing in conversation history (clean data for LLM context).

**RAG health backward-compat fix (2026-03-08):** `check_rag_health()` was missing the `$or` pattern for pre-migration claims lacking the `brain` field — showed "0/200" despite 107 claims. Fixed to use same backward-compat query as all other brain-filtered functions.

**Integration test fixes (2026-03-08):** 3 tests patched non-existent module-level functions (`save_checkpoint`/`delete_checkpoint` in `services.deferred_writer`) — fixed to `patch.object(DeferredWriter, ...)` and `patch("services.mongo_client.delete_pending_ingestion")`. 1 test relaxed for LLM non-determinism.

**Fresh start (2026-03-08):** Wiped all session data (7 sessions, 107 claims). Backup at `backups/2026-03-08/mongodb_backup.json`. Cost log preserved (272 entries). Living documents reset to clean templates.

**Tests:** 1024 unit tests, 56 integration tests across 29 test files. All unit tests run fully offline with mocks.

### Decided Against

- **Dedicated feedback ingestion UI** — feedback enters via chat (paste email + commentary) or ingestion flow (select "Investor meeting" session type). No extra UI needed.
- **Persistent book framework storage** — temporary .md upload in chat is the permanent solution. No MongoDB persistence for book content.
- **Atlas M10+ upgrade (for now)** — autoEmbed requires paid tier ($57/mo). Time-based retrieval works fine until ~200 claims. The top bar status pill monitors this automatically.
- **MongoDB backup script** — optional extra for the future. Atlas has its own backup and data volume is tiny.
- **Delete claim chat command** — edge case; if a bad claim slips through, delete directly in MongoDB.

### Spec Deviations (intentional, no action needed)
- Session metadata stored in nested `metadata` dict rather than flat top-level fields — functionally equivalent, MongoDB queries nested fields fine with dot notation.
- Sessions store `summary` (2-3 sentences) rather than a separate `one_line_summary` — serves the same purpose.
- Claims don't store `confirmed_by_user` boolean — redundant since only confirmed claims are ever stored.

### Pitch Brain Sections (13 under Current State)
Problem We're Solving, Target Market / Initial Customer, Value Proposition, Why Now, Traction / Milestones, Business Model / Revenue Model, Pricing, Go-to-Market Strategy, Technical Approach, Competitive Landscape, Moat / Defensibility, Team, Fundraising Status / Strategy. Plus: Decision Log, Dismissed Contradictions.

### Ops Brain Sections (8 top-level)
Contacts / Prospects, Active Hypotheses, Key Assumptions, Key Risks, Open Questions, Feedback Tracker, Hiring Plans, Scratchpad Notes.

### Next Steps
1. **Use the system daily** — ingest real transcripts, build up the living document, see how consistency engine performs on real data.
2. **When top bar says "RAG upgrade needed"** (~200 claims) — upgrade Atlas to M10+ and create vector search indexes per `scripts/bootstrap.py`.
