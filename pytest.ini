[pytest]
markers =
    slow: marks tests as slow (real API calls, long transcripts) — deselect with -m "not slow"
    integration: marks tests as integration tests (require API keys) — deselect with -m "not integration"
