# IMPORTANT: Range-pinned for flexibility. Pin to exact versions (==) for reproducible deploys.
# Run `pip freeze` on a working environment and pin critical deps before production.
streamlit>=1.31.0,<2.0.0
pymongo[srv]>=4.6.0,<5.0.0
anthropic>=0.40.0,<1.0.0
Pillow>=10.0.0,<11.0.0
python-dotenv>=1.0.0,<2.0.0
streamlit-cookies-controller>=0.0.4,<1.0.0
pytest>=7.0.0,<9.0.0
